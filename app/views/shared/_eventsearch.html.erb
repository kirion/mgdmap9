			<%= form_tag houses_path, :method => 'get' do %>
			  	<div class="col-md-3">
			  		<%= link_to "Добавить новое событие", new_event_path %>
			  		<div class=field>
					   	<%= label_tag "Введите адрес для поиска"%>
					    <%= text_field_tag :search, params[:search] %>
			    	</div> 
					
			    </div>
			    <div class="col-md-6">
			    	<br>
			    	<%= submit_tag "Поиск", class: "btn btn-success", :name => nil %>
			    </div> 
			<% end %>